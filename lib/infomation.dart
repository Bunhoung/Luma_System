import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class Infomation extends StatelessWidget{
  // capture personal information from HouseMember, Show when user press on the name in InfoLists
  @override
  Widget build(BuildContext context) {

    final body = ListView(
      children: [

      ],
    );

    return Scaffold(
      body: body,
    );
  }

}